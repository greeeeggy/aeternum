name: overlapped_carousel
description: A flutter package for overlapped carousel with autoscroll support
version: 1.0.2+1

environment:
  sdk: '>=2.12.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter